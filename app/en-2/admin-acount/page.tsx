import { Metadata } from "next";
import Content from "./content";

export const metadata: Metadata = {
  title: "The account - Tazawad Academy",
};

const Page = () => {
  return <Content />;
};

export default Page;
