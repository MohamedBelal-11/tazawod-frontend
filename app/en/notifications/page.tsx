import { Metadata } from "next";
import Content from "./content";

export const metadata: Metadata = {
  title: "Notifications - Tazawad Academy",
};

const Page: React.FC = () => {
  return <Content />;
};

export default Page;
