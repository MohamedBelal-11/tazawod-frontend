import { Metadata } from "next";
import Content from "./content";

export const metadata: Metadata = {
  title: "Meetings - Tazawad Academy"
}

const Page: React.FC = () => <Content />;

export default Page;
